# Forge Platform — 产品需求文档（PRD）

> **版本**: 4.0
> **日期**: 2026-04-02
> **作者**: Harvey + Claude
> **状态**: Reviewed

---

## 目录

- [1. 产品概述](#1-产品概述)
- [2. 功能需求](#2-功能需求)
- [3. 业务流程](#3-业务流程)
- [4. 业务规则](#4-业务规则)
- [5. 非功能需求](#5-非功能需求)
- [6. 约束与假设](#6-约束与假设)
- [7. 风险分析](#7-风险分析)
- [8. 名词说明](#8-名词说明)
- [附录：引用资料索引](#附录引用资料索引)

---

## 1. 产品概述

### 1.1 愿景

让完全不懂代码的产品经理/运营，通过自然语言描述需求，由 AI 驱动 **7 阶段全流程开发流水线** — 从需求澄清、风险识别、测试先行、代码生成、自动化测试、制品管理到 K8s 部署运维 — 在工程规范约束下生成生产级代码，稳定发布到生产环境。

### 1.2 定位

Forge 是一个 **AI 驱动的全流程开发平台** — 不仅仅是 AI 代码生成工具，而是覆盖从需求到部署运维全生命周期的 7 阶段流水线，为 AI Agent 提供规范约束、风险前置、测试先行、机械化验证和持续运维的完整工程化环境。

> **Harness Engineering**（OpenAI 提出）：设计约束环境、反馈循环和基础设施，使 AI 编码代理在规模化场景下可靠运行。Forge 在此基础上进一步实现了 7 阶段端到端覆盖。

本平台是面向生产环境的企业级系统，不是 MVP 原型。分阶段交付的目的是控制范围和节奏，而非降低质量标准。

### 1.3 目标用户

| 角色 | 特征 | 使用方式 |
|------|------|---------|
| 产品经理 / 运营 | 懂业务逻辑但不写代码 | 自然语言描述需求，审批发布结果 |
| 技术管理者 | 负责质量和安全兜底 | 审批高风险变更，配置规范和风险规则 |
| 平台管理员 | 负责平台运维 | 管理平台配置、外部平台接入、紧急停止 |

### 1.4 核心能力

| 能力 | 说明 |
|------|------|
| 从零孵化 | "我要一个优惠券系统" → AI 基于脚手架生成完整新项目 |
| 迭代增强 | "加一个通知渠道" → AI 理解现有架构，在正确位置插入代码 |
| 风险前置 | 需求阶段主动识别技术风险和模糊点，澄清后才进入实施 |
| 测试先行 | 先生成测试用例再写代码，原生框架运行单元/API 测试，Python 统一 E2E |
| 实时预览 | 代码生成过程实时展示文件树变化、Docker 预览环境、Lint 结果 |
| 制品管理 | Docker 镜像构建、OSS 推送、版本语义化管理 |
| K8s 部署 | 自动生成 K8s 资源清单、自动化部署、运维 Agent 持续巡检 |
| 信任分级 | 低风险全自动（生成 → Review → 合并 → 部署），高风险人工审批兜底 |
| 多入口 | Web 工作台 + 钉钉/飞书 IM + CLI，统一后端引擎 |
| 多平台适配 | 支持接入多种代码托管、容器编排、CI/CD 平台，首期对接阿里云生态 + GitHub |

### 1.5 核心理念

**规范即灵魂。** AI 只是执行者，沉淀多年的工程规范才是保证代码质量的根本。小白不需要懂代码，但 AI 必须严格遵守编码规范、安全规范和架构规范。

**测试先行。** 先写测试再写代码。单元测试和 API 测试使用目标项目原生框架（Go→go test, Java→JUnit, JS→Jest），E2E/集成测试统一使用 Python（pytest + Playwright），保证测试与业务代码深度绑定。

**风险前置。** 需求阶段 AI 主动提问澄清模糊点、识别技术风险、检测技术栈约束，在编码之前消除不确定性。

**Harness Engineering 三大支柱**：

| 支柱 | 说明 | Forge 对应 |
|------|------|-----------|
| **Context Engineering** | AI Agent 只能看到仓库内的内容 — 文档、规范、代码必须版本化存在仓库中 | 规范中心 + 项目画像 + Prompt 模板 |
| **Architectural Constraints** | 机械化强制执行架构约束，不依赖文档自觉遵守 | Linter 引擎 + 结构测试 + AI Review + 质量门禁 |
| **Entropy Management** | AI 忠实复制模式（包括坏模式），需要持续扫描和修复代码熵增 | 熵管理后台 + 自动修复 + 质量趋势 |

**反馈速度决定 Agent 自治能力**：

| 反馈层级 | 速度 | 机制 |
|---------|------|------|
| 工具调用后验证 | 毫秒级 | PostToolUse 钩子 |
| 提交前检查 | 秒级 | Pre-commit linter + 结构测试 |
| CI 流水线 | 分钟级 | 构建、测试、安全扫描 |
| 运行时反馈 | 小时级 | 可观测性数据回传 |
| 人工审查 | 天级 | 仅处理新架构决策和高风险变更 |

### 1.6 产品结构

```
Forge Platform — 7 阶段 AI 全流程开发平台
├── Phase 1: 需求澄清与风险识别 ─ 主动提问 + 风险检测 + 技术栈约束
├── Phase 2: 需求规划与任务拆分 ─ DAG 任务分解 + 需求追溯 + 优先级估算
├── Phase 3: 测试用例编写 ─────── 测试先行 + 原生框架 + Python E2E
├── Phase 4: 代码编写与实时预览 ─ 约束生成 + 实时文件树 + Docker + Lint
├── Phase 5: 自动化测试 ────────── 严格顺序执行 + 阻断门禁
├── Phase 6: 制品管理 ─────────── Docker 构建 + OSS 推送 + 版本管理
├── Phase 7: K8s 部署与运维 ───── 资源生成 + 自动部署 + 运维 Agent
│
├── 支撑系统
│   ├── AI 引擎 ————— 中央大脑：多模型路由 + LangGraph 编排
│   ├── 鉴权中心 ———— 统一身份认证 + 权限管理
│   ├── 规范中心 ———— 编码规范 + Linter 规则 + 脚手架模板 + Prompt 模板
│   ├── 约束引擎 ———— Linter 管理 + 结构测试 + Agent 可消费错误信息
│   ├── Web 工作台 ——— 用户主界面：需求输入 + 任务看板 + 审批 + 预览
│   ├── IM 机器人 ——— 钉钉/飞书入口，对话式需求提交
│   ├── 实时网关 ———— AI 流式输出 + 进度推送（SSE）
│   ├── 外部平台适配器 — 代码托管 / 容器编排 / CI/CD 的多平台抽象层
│   └── 产品加速库 ——— AI 孵化产品的可选组件库（登录/支付/订单等）
```

### 1.7 一句话端到端流程

```
需求澄清 → 风险识别 → 任务拆分(DAG) → 测试用例先行 → 约束代码生成 → 自动化测试(阻断门禁) → 制品构建(Docker+OSS) → K8s 部署 → 运维 Agent 巡检
```

---

## 2. 功能需求

### 2.1 AI 引擎

AI 引擎是平台的中央大脑，驱动 7 阶段全流程。

#### 2.1.1 需求澄清与风险识别（Phase 1）

| 功能 | 说明 |
|------|------|
| 自然语言理解 | 接收用户的自然语言需求描述，理解意图 |
| **主动提问澄清** | 需求模糊时 AI 主动提问，生成结构化澄清问题列表，而非被动等待 |
| **技术风险识别** | AI 在需求阶段即识别潜在技术风险（性能瓶颈、安全隐患、兼容性问题） |
| **技术栈约束检测** | 自动检测目标项目技术栈，确保生成方案与现有架构兼容 |
| 需求确认 | 向用户展示 AI 理解的需求 + 风险清单，用户确认后才继续 |

#### 2.1.2 需求规划与任务拆分（Phase 2）

| 功能 | 说明 |
|------|------|
| **DAG 任务分解** | 将需求拆解为有向无环图（DAG）结构的技术任务，标识依赖关系 |
| **需求追溯** | 每个任务可追溯到原始需求条目，保证完整覆盖 |
| **优先级估算** | AI 基于依赖关系和复杂度自动估算任务优先级和执行顺序 |
| 任务清单生成 | 输出结构化任务清单（改哪些模块、新增哪些接口、数据库变更） |

#### 2.1.3 代码生成（Phase 4）

| 功能 | 说明 |
|------|------|
| **约束代码生成** | 在规范约束下生成代码，实时校验 Lint 规则和架构约束 |
| 上下文感知 | 基于项目已有代码、规范、Schema 生成代码，确保与现有架构一致 |
| **实时文件树** | 生成过程中实时展示文件树变化，用户可看到新增/修改的文件 |
| **Docker 预览** | 生成完成后自动构建 Docker 预览环境，支持实时预览效果 |
| 多文件原子提交 | 一次任务生成的多个文件作为一个原子提交 |
| 独立分支 | AI 在独立分支（ai/feature-xxx）上工作，不直接操作主分支 |

#### 2.1.4 AI 代码审查

| 功能 | 说明 |
|------|------|
| 编码规范检查 | 对照规范中心规则，检查代码是否合规 |
| 安全扫描 | OWASP 规则 + SQL 注入 + XSS 检查 |
| 逻辑一致性 | 检查生成代码是否自洽、接口是否匹配 |
| 评分报告 | 输出评分（0-100）+ 问题列表 + 修复建议 |
| 自动修复 | 有问题时 AI 自动修复，最多 3 轮，超限升级人工 |

#### 2.1.5 多模型支持

| 功能 | 说明 |
|------|------|
| 多模型路由 | 按任务复杂度选择合适模型（高复杂度用强模型，简单任务用轻量模型） |
| 故障降级 | 主模型不可用时自动切换到备选模型（Claude → GPT → 通义） |
| 模型可插拔 | 新增 AI 模型不需要修改核心逻辑 |

#### 2.1.6 风险评估

| 功能 | 说明 |
|------|------|
| 两阶段评估 | 方案规划后初评 + AI 审查后终评，取较高值 |
| 风险分级 | 低风险自动流转，高风险进入人工审批 |
| 评估维度 | 需求类型、影响范围、文件数、Review 评分、是否涉及安全/支付模块 |

#### 2.1.7 并发冲突处理

| 功能 | 说明 |
|------|------|
| 独立分支隔离 | 每个任务独立分支，不直接操作 develop |
| 冲突检测 | 提交前检查目标分支状态，冲突时 AI 尝试自动解决（最多 1 次） |
| 人工兜底 | 自动解决失败时通知人工，**绝不强制覆盖人工代码** |

#### 2.1.8 DB 迁移安全

| 功能 | 说明 |
|------|------|
| 可回滚迁移 | 必须同时生成 UP 和 DOWN 迁移脚本 |
| 临时环境验证 | 先在临时环境执行验证可逆性 |
| 破坏性操作拦截 | DROP TABLE/COLUMN 等操作强制人工审批 |

---

### 2.2 测试体系

**测试先行**是 Forge 的核心理念之一：先生成测试用例，再生成业务代码。

#### 2.2.1 测试用例编写（Phase 3 — 先于代码）

| 功能 | 说明 |
|------|------|
| **测试先行** | 在代码生成之前，AI 先生成测试用例，定义预期行为 |
| **原生框架单元测试** | 单元测试使用目标项目的原生测试框架（Go→go test, Java→JUnit 5, JS→Jest） |
| **原生框架 API 测试** | API/接口测试同样使用原生框架，与项目技术栈深度绑定 |
| **Python E2E 测试** | E2E/集成测试统一使用 Python（pytest + Playwright），跨技术栈通用 |
| 测试用例覆盖 | 正常路径 + 异常路径 + 边界条件，AI 自动生成全面覆盖 |

#### 2.2.2 自动化测试执行（Phase 5）

| 测试层 | 内容 | 工具 | 执行方式 |
|--------|------|------|---------|
| 单元测试 | AI 生成的业务代码单测 | 原生框架（go test / JUnit 5 / Jest） | **严格顺序执行，失败阻断** |
| API/接口测试 | 根据 API 定义生成测试用例 | 原生框架 | **严格顺序执行，失败阻断** |
| 集成测试 | 跨服务的业务流程测试 | Python（pytest） | 可并行执行 |
| E2E 测试 | 端到端用户流程测试 | Python（pytest + Playwright） | 可并行执行 |
| 回归测试 | 运行项目已有的全部测试用例 | 原生框架 | 合并前执行 |

**阻断门禁规则**：任一测试层失败 → AI 分析失败原因 → 自动修复（最多 3 轮）→ 超限升级人工。上一层未通过不得进入下一层。

#### 2.2.3 质量门禁

| 检查项 | 要求 |
|--------|------|
| 编译通过 | 必须 |
| 单测覆盖率 | ≥ 配置阈值（默认 60%，可按项目调） |
| AI Review 通过 | 必须 |
| 安全扫描无高危 | 必须 |
| 镜像漏洞扫描 | 无 Critical 级别 |
| API 兼容性 | 不允许破坏性变更（接口删除/改签名） |

任一不通过 → 阻断部署 + AI 自动修复。

---

### 2.3 制品管理（Phase 6）

| 功能 | 说明 |
|------|------|
| **Docker 镜像构建** | AI 生成 Dockerfile，自动构建多阶段优化镜像 |
| **OSS 推送** | 镜像推送到阿里云 OSS / 容器镜像仓库 |
| **语义化版本** | 自动生成语义化版本号（SemVer），基于变更类型（feat/fix/breaking） |
| **制品元数据** | 记录构建时间、源代码 commit、测试结果、安全扫描状态 |
| **制品列表** | Web 工作台展示所有构建制品、版本历史、关联任务 |
| 镜像漏洞扫描 | 构建后自动扫描容器镜像漏洞 |

---

### 2.4 DevOps 自动化

#### 2.4.1 K8s 部署（Phase 7）

| 功能 | 说明 |
|------|------|
| **K8s 资源生成** | AI 自动生成 Deployment / Service / Ingress / ConfigMap 等 K8s 资源清单 |
| **自动化部署** | 一键部署到 ACK（阿里云 K8s），支持 dev / staging / prod 环境 |
| **运维 Agent** | 部署后运维 Agent 持续巡检：健康检查、资源利用率、异常检测 |
| **自动扩缩容** | 基于负载指标自动 HPA 扩缩容 |
| **回滚能力** | 一键回滚到上一版本，自动回滚触发规则 |

#### 2.4.2 灰度部署

| 功能 | 说明 |
|------|------|
| 金丝雀发布 | 先 5% 流量 → 观察 → 逐步放量 |
| 蓝绿部署 | 新版本就绪后一键切换 |
| 按标签分流 | 按用户标签/租户/地域分流 |
| 自动回滚 | 健康检查失败或错误率突增自动回滚 |
| 一键回滚 | 人工触发回滚到上一版本 |
| 灰度管理 UI | Web 工作台中配置灰度规则和流量比例 |

#### 2.4.3 环境管理

| 环境类型 | 说明 |
|---------|------|
| 临时环境 | AI 分支预览，MR 合并后 30min 自动销毁 |
| dev | develop 分支自动部署 |
| staging | release 分支自动部署 |
| prod | master 分支，人工审批 + 灰度部署 |

#### 2.4.4 配置自动发布

AI 生成的业务系统配置经校验（命名规范、敏感字段检测、格式正确性）后自动发布，目标服务热加载。

---

### 2.5 鉴权中心

#### 2.5.1 认证能力

| 功能 | 说明 |
|------|------|
| 多种认证方式 | 密码、OAuth2/OIDC、钉钉扫码、飞书扫码、LDAP、SSO SAML、API Token、SSH Key |
| 动态鉴权链 | 每种接入场景的认证方式可独立配置组合，运行时热更新 |
| MFA | TOTP + 短信验证码，敏感操作触发二次验证 |
| 多端登录管理 | 同时在线数控制、Token 黑名单 |

#### 2.5.2 授权能力

| 功能 | 说明 |
|------|------|
| RBAC | 基于角色（管理员/开发/PM）的权限控制 |
| 多层粒度 | 平台级 → 项目级 → 操作级 |
| 多租户 | 共享表 + tenant_id 隔离，支持全局默认 + 租户级覆盖 |
| 数据权限 | 行级/列级自动过滤 |

#### 2.5.3 接入场景

| 场景 | 认证方式 | MFA | 会话时长 |
|------|---------|-----|---------|
| Web 工作台 | 钉钉扫码 / 密码 / OAuth2 OIDC | 敏感操作时 | 8h |
| API 调用 | API Token / AK-SK 签名 | 不需要 | 按 Token 有效期 |
| Git 操作 | SSH Key / Personal Access Token | 不需要 | 按 Key 有效期 |
| IM 机器人 | IM 平台回调验签 + 用户绑定 | 不需要 | 单次会话 |
| CLI 工具 | OAuth2 Device Flow / Personal Access Token | 不需要 | 按 Token 有效期 |

#### 2.5.4 权限模型

```
租户 (Tenant)
  └── 组织 (Organization)
       └── 项目空间 (Project Space)
            ├── 代码仓库  —— 读 / 写 / 管理
            ├── 流水线    —— 查看 / 触发 / 编辑
            ├── 环境      —— dev / staging / prod 各自独立
            └── AI 任务   —— 提交 / 审批 / 查看
```

---

### 2.6 Web 工作台

#### 2.6.1 导航结构

采用**项目优先**的导航模式 — 用户先选择项目，再进入项目子页面。不同角色看到不同的页面集合。

#### 2.6.2 项目管理

| 功能 | 说明 |
|------|------|
| **一键接入** | OAuth 授权 GitHub/Codeup，一次性同步全部仓库，用户选择关注的项目 |
| **创建新项目** | 选模板快速开始 + 分步引导自定义（代码托管/部署环境/CI/CD 平台） |
| **项目画像** | 导入已有项目时 AI 自动分析（技术栈、架构、数据库 Schema），生成项目画像 |
| **项目列表** | 星标置顶 + 按 Organization 分组 + 搜索筛选 |

#### 2.6.3 页面清单

**产品经理视图**：

| 页面 | 功能 |
|------|------|
| **登录页** | 账号密码登录（Phase 1），后续增加钉钉扫码/OAuth2 |
| **需求对话** | 混合式交互：自然语言输入 → AI 主动澄清 + 风险提示 → 需求确认卡片 → 确认执行 |
| **任务看板** | 三级进度视图可切换：概览（Kanban）/ 详情（关键产出）/ 实时（流式输出） |
| **AI 工作过程** | 7 阶段时间线 + 实时工作区，可看 AI 当前步骤、决策原因、实时代码生成 |
| **变更结果** | 三层展示：AI 总结 + 信任指标 → 变更摘要 → 代码详情（AI Diff + 逐行解释） |
| **测试报告** | 测试结果：单测 / API 测试 / 集成测试 / E2E 测试 / 回归测试 |
| **制品管理** | Docker 镜像列表、版本历史、构建状态、关联任务 |
| **部署环境** | K8s 环境状态总览（dev/staging/prod）、发布记录、运维 Agent 状态 |

**技术管理者额外视图**：

| 页面 | 功能 |
|------|------|
| **分支管理** | 活跃分支、冲突检测、合并记录、手动干预 |
| **MR 审批** | AI Review 报告 + 风险评分 + 审批操作 |
| **质量大盘** | AI 生成代码合规率、Review 通过率、线上故障率 |
| **规范配置** | 项目级规范覆盖、Review 规则、风险阈值 |

**平台管理员视图（全局）**：

| 页面 | 功能 |
|------|------|
| **系统设置** | AI 模型配置、紧急停止控制台、用户管理 |
| **规范中心** | 全局编码规范、Prompt 模板、脚手架管理 |
| **适配器管理** | 平台凭证、健康状态监控 |

#### 2.6.4 代码可视化增强

不自建 Git 存储引擎、代码搜索等基础能力（由代码托管平台提供），做体验增强：

- AI Diff 智能注释（每段变更附解释）
- 风险标注（高亮可能有问题的代码行）
- AI 对话式 Code Review（在代码行上提问）
- 变更影响分析（这次改动影响哪些服务/接口）

#### 2.6.5 AI 工作过程可视化

AI 执行过程对用户完全透明，采用"7 阶段时间线 + 实时工作区"结构：
- 时间线展示 7 个阶段的状态、耗时、一句话摘要
- 实时工作区根据当前阶段动态展示内容（澄清问答、任务 DAG、测试用例、代码流式生成、测试进度、制品构建、部署状态）
- AI 做重要决策时展示决策卡片（原因 + 确认按钮），高风险决策阻塞等待用户确认
- 技术管理者可展开查看原始对话记录和工具调用详情

> 详细设计参见 [product-design.md](product-design.md)

---

### 2.7 IM 机器人

| 功能 | 说明 |
|------|------|
| 支持平台 | 钉钉（企业内部机器人）、飞书（自建应用） |
| 交互方式 | 群聊 @forge / 私聊 |
| 需求提交 | @forge + 需求描述 → 结构化理解 → 用户确认 → 创建任务 |
| 进度推送 | 实时进度卡片（含进度条和当前步骤） |
| 结果通知 | AI Review 评分、风险等级、操作按钮 |
| 紧急停止 | 支持通过 IM 触发 L1 级别停止 |

---

### 2.8 实时网关

| 功能 | 说明 |
|------|------|
| AI 流式输出 | 代码生成过程实时展示给用户（SSE） |
| 任务进度推送 | 7 阶段状态变更实时推送 |
| 审批通知 | 需要人工审批时实时通知 |
| 部署进度 | 构建/推送/上线状态实时推送 |
| 系统告警 | 紧急停止等系统级通知 |
| 断线恢复 | 短时间断线重连后恢复会话 |

---

### 2.9 规范中心

#### 2.9.1 定位

规范中心是 AI 代码生成质量的根本保障，不只是静态文件，而是可管理、可演进、可度量的规范服务。

#### 2.9.2 内容结构

| 内容 | 说明 |
|------|------|
| 编码规范 | Java、SQL、Redis、Kafka、API 设计、安全编码、命名规范、Git 工作流 |
| 脚手架模板 | Java 微服务骨架、API 网关骨架、Vue 3 前端骨架、Java SDK 骨架 |
| AI Review 规则 | 编码规约、安全（OWASP Top 10）、性能、数据库规范、API 兼容性、团队自定义 |
| Prompt 模板 | 核心模板（详见 2.9.4） |
| 规范测试套件 | bad-code-samples + good-code-samples + eval 测试 |

#### 2.9.3 编码规范基线

以已有工程实践沉淀为基线（详见 [references/coding-standards.md](references/coding-standards.md)），核心要点：

| 规范维度 | 要求 |
|---------|------|
| 领域模型命名 | DO / DTO / VO / BO 严格区分 |
| 响应封装 | 统一 Result\<T> 包装 |
| 异常体系 | 分层：BizException / SysException / 领域异常 |
| 错误码 | 集中式 ErrorCode 枚举，禁止硬编码 |
| 依赖注入 | 构造器注入，禁止字段注入 |

#### 2.9.4 Prompt 模板体系

**Prompt 是平台最核心的资产，必须在开发前完成设计、评审和 eval 测试。**

每个 Prompt 调用由四层叠加：

| 层级 | 内容 | 变化频率 |
|------|------|---------|
| System Prompt（固定层） | AI 角色定义、行为约束、输出格式 | 极少变化 |
| Standards Injection（项目层） | 编码规范、技术栈约束、命名规则 | 按项目不同 |
| Context（任务层） | 相关代码、Schema、API 契约、变更历史 | 每次任务 |
| User Input（用户层） | 需求描述、澄清回复、修改要求 | 每次交互 |

**核心 Prompt 模板**：

| 模板 | 用途 | 输入 | 输出 |
|------|------|------|------|
| requirement-analysis | 需求澄清 + 风险识别 | 用户需求 + 项目上下文 | 澄清问题 + 风险清单 + 任务 DAG |
| test-generation | 测试先行生成 | 任务清单 + API 定义 + 规范 | 测试用例代码（原生框架 + Python E2E） |
| code-generation | 约束代码生成 | 任务清单 + 测试用例 + 相关代码 + 规范 | 可提交的代码文件 |
| code-review | 代码审查 | 生成代码 + 规范 + Review 规则 | 评分 + 问题 + 修复建议 |
| fix-generation | 修复生成 | Review/测试反馈 + 原始代码 + 规范 | 修复后的代码 |
| doc-generation | 文档生成 | 代码 + API 定义 | API 文档 + 变更日志 |

**质量保障**：每个 Prompt 配套 eval 测试，修改后与上一版本 A/B 对比确保不退化。

#### 2.9.5 规范继承机制

```
公司级规范（默认）→ 团队级覆盖（可选）→ 项目级覆盖（可选）→ 最终生效规范
```

#### 2.9.6 规范效果度量

持续追踪：AI 生成代码合规率、AI Review 准确率、自动合并代码线上故障率、Prompt 生成质量评分。形成反馈闭环。

---

### 2.10 产品加速库

#### 2.10.1 定位

**AI 孵化产品的可选组件库**，不是 Forge 平台自身的基座。消费者是 AI 生成的业务系统，核心价值是避免重复造轮子。

#### 2.10.2 能力清单

**技术基础设施层**（中间件薄封装）：

| 组件 | 能力 |
|------|------|
| 统一响应 + 异常 | Result\<T>、ErrorCode、分页、断言 |
| 数据库集成 | ORM + 多数据源 + 迁移管理 |
| 缓存集成 | 分布式缓存 |
| 消息队列抽象 | 统一 API，多实现可切换 |
| 对象存储 | 多云支持 |
| 日志 + 指标 | 统一日志格式 + 指标采集 |

**业务能力层**（通用业务模块）：

| 模块 | 能力 |
|------|------|
| 登录认证 | 多种登录方式集成 |
| 会员体系 | 用户等级、积分、权益 |
| 支付集成 | 多渠道支付 |
| 订单引擎 | 通用订单生命周期 |
| 通知中心 | 短信/邮件/IM/站内信 |

---

### 2.11 外部平台适配

#### 2.11.1 适配需求

系统需支持接入多种外部平台，通过统一抽象层屏蔽差异，首期对接 GitHub + 阿里云生态。

| 适配类型 | 职责 | 首期接入 | 未来扩展 |
|---------|------|---------|---------|
| 代码托管 | Git 仓库读写、分支管理、MR、Webhook | GitHub（已实现）、阿里云 Codeup | Gitee、GitLab |
| 容器编排 | K8s 资源管理、环境管理、部署 | 阿里云 ACK | 原生 K8s、腾讯 TKE、华为 CCE |
| CI/CD 流水线 | 流水线创建/触发/查询/日志 | GitHub Actions | 云效 Flow、Jenkins、GitLab CI |
| 可观测性 | 指标/追踪/日志/性能剖析 | Grafana + Loki + Prometheus | DeepFlow（eBPF）、Datadog |
| 容器镜像仓库 | 镜像存储、版本管理 | 阿里云 OSS / ACR | Docker Hub、Harbor |

#### 2.11.2 业务要求

- 项目创建时选择平台组合，项目生命周期内保持一致
- 同一 Forge 实例可同时服务不同平台组合的项目
- 新增平台支持不需要修改核心业务逻辑
- 各平台的限流、容错、缓存对上层调用者透明
- 所有平台凭证统一加密管理

---

### 2.12 Harness Engineering 能力

> 参考调研：[references/harness-engineering-research.md](references/harness-engineering-research.md)

#### 2.12.1 机械化约束引擎

| 功能 | 说明 |
|------|------|
| Linter 规则管理 | 项目级自定义 linter 规则，继承公司级默认规则 |
| 结构测试 | 验证代码分层架构（依赖方向、横切关注点入口等） |
| Agent 可消费错误 | Linter/结构测试失败时，错误信息设计为 AI Agent 可直接消费的格式，引导自修复 |
| 约束执行时机 | 代码生成后立即执行（秒级反馈），不等到 CI 流水线 |
| 约束配置 UI | 技术管理者可在 Web 工作台配置项目的架构约束规则 |

#### 2.12.2 熵管理系统

| 功能 | 说明 |
|------|------|
| 定期代码扫描 | 后台定时扫描代码库，检测命名漂移、文档过时、死代码、测试覆盖下降 |
| 自动修复 PR | 发现问题后自动生成修复 PR，低风险自动合并，高风险等待审批 |
| 质量趋势追踪 | 长期追踪代码质量指标趋势（规范合规率、复杂度、覆盖率） |
| 熵告警 | 代码质量指标下降超过阈值时主动告警 |
| 文档同步检查 | 代码变更后检查相关文档是否同步更新 |

#### 2.12.3 可观测性集成

| 功能 | 说明 |
|------|------|
| 平台自身监控 | Grafana + Loki + Prometheus 监控 Forge 平台各组件 |
| AI 孵化产品监控 | 通过可观测性适配器为 AI 生成的产品提供监控能力 |
| 运行时指标展示 | Web 工作台展示 AI 生成代码的运行时指标（延迟、吞吐、错误率、资源消耗） |
| 反馈闭环 | 可观测性数据自动回传 AI 引擎，作为迭代任务的上下文 |
| 部署后验证 | 部署后自动检查运行时健康状态，异常时触发告警或自动回滚 |

#### 2.12.4 多层反馈加速

| 功能 | 说明 |
|------|------|
| PostToolUse 钩子 | AI Agent 每次工具调用后毫秒级验证结果 |
| Pre-commit 钩子 | 提交前秒级运行 linter 和结构测试 |
| CI 快速反馈 | 优化 CI 流水线执行速度，提供分钟级反馈 |
| 可观测性反馈 | 部署后小时级提供运行时数据反馈 |

---

## 3. 业务流程

### 3.1 核心流程 — 7 阶段 AI 开发流水线

以"给优惠券系统加一个按用户等级发放的功能"为例：

```
① 需求澄清与风险识别（Phase 1）
   用户通过 Web 工作台 / IM 机器人 输入自然语言需求
   → AI 主动提问澄清模糊点（"按等级发放是指不同等级不同面额，还是不同等级不同可领取数量？"）
   → AI 识别技术风险（"涉及优惠券核心发放逻辑，需注意并发扣减安全"）
   → AI 检测技术栈约束（"目标项目为 Java + Spring Boot，数据库 MySQL"）
   → 反馈给用户确认：需求理解 + 风险清单

② 需求规划与任务拆分（Phase 2）
   AI 加载项目上下文（规范 + 代码 + 架构），生成 DAG 任务图
   → 标识任务依赖关系和执行顺序
   → 每个任务追溯到原始需求条目
   → 初步风险评估

③ 测试用例编写（Phase 3 — 先于代码）
   AI 先生成测试用例，定义预期行为
   → 单元测试：JUnit 5（与项目原生框架一致）
   → API 测试：JUnit 5 + MockMvc
   → E2E 测试：Python pytest + Playwright
   → 用户确认测试覆盖范围

④ 代码编写与实时预览（Phase 4）
   基于测试用例和规范约束生成实现代码
   → 实时展示文件树变化
   → 实时 Lint 检查
   → Docker 预览环境自动构建
   → 原子提交到独立分支
   → AI Review：编码规范 + 安全扫描 + 逻辑一致性
   → 有问题自动修复（最多 3 轮）
   → 终评风险等级

⑤ 自动化测试（Phase 5）
   严格顺序执行，上一层未通过不进入下一层：
   单元测试(go test/JUnit) → API 测试 → 集成测试(pytest) → E2E 测试(pytest+Playwright) → 回归测试
   → 失败时 AI 分析日志并修复（最多 3 轮）
   → 3 轮未通过升级人工

⑥ 制品管理（Phase 6）
   Docker 镜像构建（多阶段优化）
   → 镜像漏洞扫描
   → 语义化版本号生成
   → 推送到 OSS / 容器镜像仓库
   → 记录制品元数据

⑦ K8s 部署与运维（Phase 7）
   AI 生成 K8s 资源清单（Deployment/Service/Ingress/ConfigMap）
   → 低风险 → 自动部署到 dev/staging
   → 高风险 → 创建 MR → 人工审批后部署
   → 生产 → 必须人工审批 + 灰度发布
   → 运维 Agent 持续巡检：健康检查、资源利用率、异常检测
   → 异常时自动告警或回滚

   全程实时推送进度，用户可在任意环节介入、暂停、取消
```

### 3.2 任务生命周期

```
SUBMITTED → ANALYZING → PLANNING → TEST_WRITING → GENERATING → REVIEWING → TESTING → BUILDING → DEPLOYING → DONE
                                                      │                       │          │
                                                 HUMAN_REVIEW             ROLLBACK   BUILD_FAILED
                                                      │
                                                APPROVED / REJECTED
```

- 任何环节失败可从失败点重试（最多 3 轮，超限升级人工）
- HUMAN_REVIEW 是风险分级关键节点 — 低风险跳过，高风险卡住等人审批
- TEST_WRITING 阶段在 GENERATING 之前，体现测试先行

### 3.3 IM 交互流程

```
用户 @forge + 需求描述
  → 机器人回复结构化理解 + 风险提示，请用户确认
  → 确认后创建任务，实时推送 7 阶段进度卡片
  → 完成后推送结果卡片（评分 + 风险等级 + 制品版本 + 操作按钮）
```

---

## 4. 业务规则

### 4.1 风险分级规则

**低风险判定**（全部满足才算低风险）：
- 纯配置/文档/测试变更，或新增独立接口
- 影响文件数 ≤ 5
- AI Review 评分 ≥ 90
- 不涉及安全/支付/核心业务逻辑

**高风险**（任一命中）：
- 修改核心业务逻辑 / 数据库 Schema 变更 / 涉及权限/支付/安全
- 影响文件数 > 10
- AI Review 评分 < 90 或有告警

### 4.2 三级紧急停止开关

| 级别 | 名称 | 影响范围 | 触发方式 | 恢复方式 |
|------|------|---------|---------|---------|
| L1 | 暂停提交 | 阻断新任务，已执行任务继续 | Web / IM / API | 原路解除 |
| L2 | 冻结引擎 | 任务暂停，阻断代码提交和部署 | Web / API | 手动恢复 + 逐任务确认 |
| L3 | 全面停机 | 切断所有入口 + 停止所有工作负载 | 仅 API | 双人确认后逐步恢复 |

**自动触发规则**：
- 连续 3 次部署失败且错误率 > 5% → 自动触发 L1
- 1 小时内回滚次数 > 3 → 自动触发 L2

### 4.3 失败处理规则

**原则：禁止静默失败。**

| 失败场景 | 最大重试 | 处理 | 用户通知 |
|---------|---------|------|---------|
| AI 代码生成失败 | 3 轮 | AI 自动修复，超限放弃 | "AI 无法完成，已转交开发团队" + 部分代码 + 失败原因 |
| 测试执行失败 | 3 轮 | AI 分析失败原因并修复代码/测试 | 失败测试用例 + 原因分析 |
| 制品构建失败 | 3 轮 | AI 分析 Dockerfile/构建日志并修复 | 错误日志摘要 |
| 部署失败 | 1 次回滚 | 自动回滚 | "部署失败，已自动回滚" + 日志 |
| 外部平台 API 失败 | 3 次 | 退避重试 | 超限后通知任务暂停 |

每次失败通知必须包含：失败环节、原因描述、已完成的工作、下一步建议。

### 4.4 成本控制规则

| 层级 | 机制 | 说明 |
|------|------|------|
| 任务级 | 预估展示 | 提交前展示预估消耗和费用，用户确认后执行 |
| 任务级 | 硬上限 | 单次任务 Token 上限（默认 1M），超限中止 |
| 租户级 | 月度预算 | 80% 软告警，100% 硬限制 |
| 平台级 | 并发控制 | 全局最大并发 AI 任务数，队列排队 |

### 4.5 多租户隔离规则

- 所有数据按 tenant_id 隔离
- 鉴权配置支持全局默认 + 租户级覆盖
- 后续如有大租户需要独立部署，再做更强隔离

### 4.6 并发冲突规则

- AI 永远不会强制覆盖人工代码
- 冲突时优先保护人工变更
- 冲突自动解决失败 → 标记 CONFLICT → 通知人工

---

## 5. 非功能需求

### 5.1 可用性

| 指标 | 要求 |
|------|------|
| 平台可用性 | 99.9%（排除计划维护窗口） |
| AI 引擎故障恢复 | 主节点故障秒级切换到备节点 |
| 任务不丢失 | 任何组件重启后可从中断点恢复未完成任务 |
| 长任务断点续传 | 任务拆分为 checkpoint，重启后从最后 checkpoint 继续 |

### 5.2 性能

| 指标 | 要求 |
|------|------|
| 需求响应 | 用户提交后 3 秒内开始 AI 流式输出 |
| 代码生成 | 典型任务（5-10 文件变更）10 分钟内完成 |
| 测试执行 | 单元/API 测试 < 5 分钟，E2E 测试 < 15 分钟 |
| 制品构建 | Docker 镜像构建 < 10 分钟 |
| 弹性扩缩 | 任务并发量增加时自动扩容 Worker |

### 5.3 安全

| 要求 | 说明 |
|------|------|
| 传输加密 | 全链路 HTTPS / WSS |
| 凭证管理 | 所有密钥和凭证加密存储，定期轮转 |
| AI 安全审查 | 每次生成代码必须经过安全扫描 |
| 数据隔离 | 租户间数据完全隔离 |
| 审计日志 | 所有敏感操作可追溯 |
| 容器安全 | Docker 镜像构建后自动漏洞扫描 |

### 5.4 可观测性

| 要求 | 说明 |
|------|------|
| Forge 平台自身监控 | 全量服务 metrics + 告警（Grafana + Prometheus） |
| 结构化日志 | 集中采集和检索（Loki） |
| 链路追踪 | 跨服务调用全链路可追溯 |
| AI 用量追踪 | 每次模型调用记录 Token、费用、延迟 |
| **运行时反馈闭环** | 可观测性数据（延迟、错误率、资源消耗）回传 AI 引擎，作为迭代上下文 |
| **部署后验证** | 部署后自动检查运行时健康状态，异常时触发告警或自动回滚 |

### 5.5 可扩展性

| 要求 | 说明 |
|------|------|
| 外部平台 | 新增代码托管/容器编排/CI/CD 平台不需改核心逻辑 |
| AI 模型 | 新增模型只需实现统一接口 |
| 鉴权方式 | 新增认证方式只需实现插件 |
| 规范 | 规范支持公司级 → 团队级 → 项目级继承覆盖 |
| 测试框架 | 新增目标语言测试框架只需扩展测试生成模板 |

---

## 6. 约束与假设

### 6.1 技术约束

| 约束 | 说明 |
|------|------|
| 首期云厂商 | 阿里云生态（ACK + OSS），代码托管已支持 GitHub |
| 后端语言 | Go (API Server / DevOps Worker / Constraint Worker / IM Bot) + Python (AI Worker / LangGraph 编排) |
| 前端框架 | Next.js 15 (React) + TypeScript |
| 工作流引擎 | Temporal（状态管理 / Checkpoint / 编排 / 恢复） |
| 数据库 | PostgreSQL（单实例 HA，多 Schema 隔离） |
| 密钥管理 | HashiCorp Vault（API Key / 凭证加密存储 + 自动轮转） |
| 配置管理 | K8s ConfigMap + Vault（不使用独立配置中心） |
| 容器运行时 | Docker（镜像构建）+ K8s（编排部署） |
| 测试框架 | 原生框架（go test / JUnit 5 / Jest）+ Python（pytest + Playwright） |

### 6.2 业务假设

| 假设 | 说明 |
|------|------|
| AI 能力边界 | AI 可生成符合规范的 CRUD + 中等复杂度业务代码；复杂算法/高并发核心模块仍需人工 |
| 用户配合 | 用户愿意通过多轮对话澄清需求 |
| 规范先行 | 编码规范和 Prompt 模板在开发前完成设计和 eval 测试 |
| 网络环境 | 平台部署在可访问阿里云 API 和 AI 模型 API 的网络环境 |
| 基础设施就绪 | ACK（K8s）和阿里云 OSS 已可用 |

---

## 7. 风险分析

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| AI 生成代码质量不达标 | 高 | 高 | 测试先行 + 多轮 Review + 修复循环；规范基线 + Prompt eval |
| AI 生成测试用例覆盖不足 | 中 | 高 | 覆盖率门禁 + AI 自动补充边界用例 + 人工审查 |
| AI 模型 API 不稳定/限流 | 中 | 高 | 多模型 fallback；请求排队；错峰调度 |
| 外部平台 API 限流或不可用 | 中 | 高 | 适配器内置缓存 + 限流 + 重试 |
| 小白用户需求描述模糊 | 高 | 中 | AI 主动提问澄清；风险前置识别；需求模板引导 |
| AI 生成不安全代码 | 中 | 高 | 专项安全 Review 规则；安全模块强制人工审批；安全扫描门禁 |
| Token 费用失控 | 中 | 高 | 任务级预估 + 租户月度预算 + 硬上限中止 |
| AI 生成不可逆 DB 迁移 | 中 | 高 | 必须 UP+DOWN 迁移；临时环境验证可逆性；破坏性操作强制人工审批 |
| 生产事故 | 低 | 极高 | 三级紧急停止开关；灰度 + 自动回滚；运维 Agent 巡检 |
| Docker 镜像安全漏洞 | 中 | 高 | 构建后自动漏洞扫描；Critical 级别阻断部署 |
| K8s 资源配置不当 | 中 | 高 | AI 生成资源清单经模板校验 + 资源限制默认值 |
| 上下文窗口不足 | 中 | 中 | 摘要 + 向量检索 + Token 预算裁剪 |
| 适配器抽象泄漏 | 中 | 中 | 最小公共能力集设计；扩展第二套适配器时验证抽象合理性 |
| 多平台凭证管理复杂度 | 低 | 高 | 统一加密存储；凭证过期监控；适配器健康检查 |
| 多租户数据泄露 | 低 | 极高 | 自动注入 tenant_id；统一隔离机制；定期安全审计 |

---

## 8. 名词说明

| 术语 | 说明 |
|------|------|
| 7 阶段流水线 | Forge 的核心开发流程：需求澄清 → 任务拆分 → 测试先行 → 代码生成 → 自动化测试 → 制品管理 → K8s 部署 |
| Harness Engineering | OpenAI 提出的工程范式 — 设计约束环境、反馈循环和基础设施，使 AI Agent 在规模化场景下可靠运行 |
| Context Engineering | Harness 的子集 — 优化 AI Agent 能看到的所有上下文（代码、文档、规范、可观测性数据） |
| 测试先行 | 先生成测试用例再生成业务代码，测试定义预期行为，代码实现行为 |
| 原生测试框架 | 目标项目自身的测试框架（Go→go test, Java→JUnit 5, JS→Jest），而非外部测试平台 |
| DAG 任务分解 | 将需求拆解为有向无环图结构的任务，明确依赖关系和执行顺序 |
| 风险前置 | 在需求阶段即识别技术风险，而非等到代码生成后才发现问题 |
| 机械化约束 | 通过 linter、结构测试等确定性工具强制执行架构规范，不依赖 AI 自觉遵守 |
| 熵管理 | 定期扫描和修复代码库中的质量偏移（命名漂移、文档过时、死代码等） |
| 制品 | Docker 镜像及其元数据（版本号、构建时间、源代码 commit、测试/安全扫描结果） |
| 运维 Agent | 部署后持续运行的智能巡检代理，监控健康状态、资源利用率、异常检测 |
| 孵化 | 通过 Forge 平台 AI 从零生成一个新的业务系统 |
| 编排服务 | AI 引擎的核心调度组件，驱动任务状态机 |
| 风险等级 | AI 对代码变更的安全评级，决定是否需要人工审批 |
| 产品加速库 | AI 孵化产品可选引用的通用组件库（登录/支付/订单等） |
| 适配器 | 屏蔽外部平台差异的抽象层，上层调用统一接口 |
| 灰度发布 | 新版本按比例/规则逐步放量的发布策略 |
| Prompt 模板 | 预定义的 AI 提示词结构，保证生成代码的质量和一致性 |
| 紧急停止开关 | 三级应急机制，可快速阻断 AI 代码生成和部署 |
| HUMAN_REVIEW | 高风险变更的人工审批环节 |
| SSE | Server-Sent Events — 服务器向客户端推送实时事件的协议 |

---

## 附录：关联文档

| 文档 | 说明 |
|------|------|
| [product-design.md](product-design.md) | 产品设计规格书 — 页面详细设计、交互流程、视觉规范 |
| [technical-design.md](technical-design.md) | 技术设计文档 — 架构、数据模型、高可用、技术选型 |
| [milestone-plan.md](milestone-plan.md) | 里程碑计划 — 分阶段交付路线图 |

## 附录：引用资料索引

以下资料提炼自已有工程实践和行业调研，供设计和实施参考。

| 文件 | 内容 | 来源 |
|------|------|------|
| [references/harness-engineering-research.md](references/harness-engineering-research.md) | Harness Engineering 调研报告（OpenAI + DeepFlow） | OpenAI 博客 + 开源社区 |
| [references/coding-standards.md](references/coding-standards.md) | 编码规范基线（核心提炼） | aegis 工程实践 |
| [references/scaffold-patterns.md](references/scaffold-patterns.md) | 脚手架设计范式（starter 模式、自动装配、条件激活） | solar-foundation 架构模式 |
| [references/gray-release-methodology.md](references/gray-release-methodology.md) | 灰度发布方法论（三层决策模型） | kohinur 灰度发布理念 |
| [references/codeup-api.md](references/codeup-api.md) | 代码托管首期实现参考 — Codeup API 能力清单 | 阿里云云效文档 |
| [references/ack-api.md](references/ack-api.md) | 容器编排首期实现参考 — ACK/K8s API 能力清单 | 阿里云 ACK 文档 |
