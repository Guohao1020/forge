name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  go-test:
    name: Go Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: forge-core

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: forge-core/go.sum

      - name: Build
        run: go build ./cmd/forge-core

      - name: Vet
        run: go vet ./internal/...

      - name: Unit Tests
        run: |
          go test ./internal/middleware/... \
                  ./internal/module/auth/... \
                  ./internal/module/version/... \
                  ./internal/module/specs/... \
                  ./internal/temporal/... \
                  ./internal/pkg/... \
                  -count=1 -race

      - name: Detector Tests
        run: go test ./internal/module/project/... -run TestDetect -count=1

  python-test:
    name: Python Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-worker

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: ai-worker/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Unit Tests with Coverage
        run: |
          python -m pytest tests/ \
            --cov=src \
            --cov-report=term \
            --cov-fail-under=50 \
            --tb=short \
            -q

  typescript-check:
    name: TypeScript Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: forge-portal

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: forge-portal/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type Check
        run: npx tsc --noEmit

      - name: ESLint
        run: npx eslint app/ components/ lib/ --max-warnings 50

      - name: Build
        run: npm run build

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [go-test, python-test, typescript-check]

    steps:
      - uses: actions/checkout@v4

      - name: Build forge-core
        run: docker build -t forge-core:ci ./forge-core

      - name: Build ai-worker
        run: docker build -t forge-ai-worker:ci ./ai-worker

      - name: Build forge-portal
        run: docker build -t forge-portal:ci ./forge-portal

      - name: Verify images
        run: |
          docker images --format "{{.Repository}}:{{.Tag}} {{.Size}}" | grep ":ci"
